export const dynamic = 'force-dynamic';

export default function DashboardPage() {
  // The dashboard layout handles the visual shell.
  // This page is intentionally minimal — content is rendered
  // by the layout's sidebar, calendar, and task list sections.
  return null;
}
